{
  "name": "bbacs-pi-test",
  "description": "BB ACS Node on the Raspberry Pi",
  "scripts": {
    "preinstall": "bash deps.sh"
  },
  "version": "0.0.3",
  "dependencies": {
    "request": "^2.55.0"
  },
  "engines": {
      "node": "0.10.36"
  }
}
